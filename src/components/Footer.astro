---
import { NavItems } from "@/lib/consts";
---

<footer
  class="flex flex-col w-full md:justify-center items-center py-24 md:px-48 gap-8 mx-auto bg-[#965d24] text-white"
>
  <div class="flex flex-col items-center md:items-start justify-between w-full">
    <h4 class="text-[clamp(2rem,3vw,5rem)] text-[#dfb968] uppercase font-fancy">
      Leon Photography
    </h4>
    <ul class="flex flex-col items-start flex-wrap gap-2">
      {
        NavItems.map((item) => (
          <li>
            <a href={item.href}>{item.text}</a>
          </li>
        ))
      }
    </ul>
  </div>
  <p class="self-start">© 2025 Leon Photography</p>
</footer>
