---
import { NavItems } from "@/lib/consts";
---

<footer
  class="flex flex-col w-full md:justify-center items-center container py-24 max-w-3xl px-4 gap-8"
>
  <div
    class="flex flex-col items-center md:flex-row md:items-start justify-between w-full"
  >
    <h4 class="text-[clamp(2rem,3vw,5rem)] text-[#dfb968] uppercase">
      Leon Photography
    </h4>
    <ul class="flex flex-col items-center flex-wrap gap-2">
      {
        NavItems.map((item) => (
          <li>
            <a href={item.href}>{item.text}</a>
          </li>
        ))
      }
    </ul>
  </div>
  <p class="self-start">© 2025 Leon Photography</p>
</footer>
